# Maybe
mkdir -p tests &&  ../dist/build/psc/psc Maybe.ps -o tests/Maybe.js && cat tests/Maybe.js Maybe.js | node
<<<
>>>
{ ctor: 'Just', value: 'ab' }
>>>2
>>>= 0

# List
mkdir -p tests &&  ../dist/build/psc/psc List.ps -o tests/List.js && cat tests/List.js List.js | node
<<<
>>>
[ 1, 2 ]
>>>2
>>>= 0

# Foreach
mkdir -p tests &&  ../dist/build/psc/psc Foreach.ps -o tests/Foreach.js && cat tests/Foreach.js Foreach.js | node
<<<
>>>
15
>>>2
>>>= 0
